// import { effect, z } from "zod";
// import { os } from "~/route";

// const DrawCard = os.input(z.object({})).handler(async ({ input }) => {
//   return { id: input.id, name: "User " + input.id };
// });
